# Streamlit UI
streamlit>=1.32.0

# LangChain & OpenAI (updated 2025-10-21)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-text-splitters>=0.0.1
openai>=1.12.0

# Gemini (Google AI Studio SDK - API Key方式)
google-generativeai>=0.3.0

# Vector Database
# chromadb>=0.4.22  # Supabase使用中のため不要（約50MB削減）
supabase>=2.0.0
pgvector>=0.2.0

# PDF Processing
pdfplumber>=0.10.3
pdf2image>=1.16.0  # For PDF page to image conversion
PyMuPDF>=1.23.0  # For fast PDF text search and coordinate extraction
streamlit-pdf-viewer>=0.0.26  # For PDF display with annotations

# Word Processing
python-docx>=1.0.0  # For .docx and .doc files

# Excel Processing
openpyxl>=3.1.0  # For .xlsx and .xls files

# PowerPoint Processing
python-pptx>=1.0.0  # For .pptx and .ppt files

# Image Processing
Pillow>=10.2.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.6.0
PyYAML>=6.0.1

# Observability
langfuse>=2.0.0,<3.0.0

# Reranking (Phase 1: Performance Improvement)
sentence-transformers>=2.7.0
cohere>=5.0.0  # Optional: For Cohere Rerank API (better Japanese support)

# BM25 Hybrid Search (Phase 1: Performance Improvement)
rank-bm25>=0.2.2
mecab-python3>=1.0.6  # Optional: For Japanese tokenization

# Hybrid Highlighting (for embedding similarity calculation)
scikit-learn>=1.3.0

# Updated: 2025-11-10 - Fixed PowerPoint highlighting with database chunks
